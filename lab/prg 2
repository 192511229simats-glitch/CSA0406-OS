#include <fcntl.h>
#include <unistd.h>
int main()
{
    int s = open("a.txt", O_RDONLY);
    int d = open("b.txt", O_WRONLY | O_CREAT | O_TRUNC, 0644);
    char c;
    while (read(s, &c, 1))
        write(d, &c, 1);
    close(s);
    close(d);
}

input:
Hello

output:
Hello
